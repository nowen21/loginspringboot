server:
  port: 8087  # Puerto en el que se ejecutará la aplicación

spring:
  application:
    name: softcontab
  datasource:
    url: jdbc:mysql://localhost:3306/softcontab?useSSL=false&serverTimezone=UTC
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true